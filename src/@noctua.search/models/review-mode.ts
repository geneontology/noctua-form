export enum ReviewMode {
    off = 'off',
    on = 'on',
    locked = 'locked'
}

export enum ArtReplaceCategory {
    term = 'term',
    gp = 'gp',
    reference = 'reference'
}

export enum ReloadType {
    RESET = 'reset',
    STORE = 'store'
}